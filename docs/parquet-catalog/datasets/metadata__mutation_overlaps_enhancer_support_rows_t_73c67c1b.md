<!-- generated by scripts/parquet_catalog.py; do not edit -->
# `metadata__mutation_overlaps_enhancer_support_rows_t_73c67c1b`

- Layer: `metadata`
- Status: **canonical present**
- As of: `2026-06-24`
- Canonical URI/pattern: `gs://jouvencekb/kg/v2/metadata/mutation_overlaps_enhancer_support_rows_t_73c67c1b.parquet`
- Logical rows: **10,501**
- Physical objects: **1**
- Bytes: **400,857**
- Schema hash (`arrow-fields-v1`): `70072b8263d1b09cf6458a1f1ff1a7132d473e88879a0ee3bf92eb95049d8cef`

## Meaning

Canonical public `mutation_overlaps_enhancer_support_rows_t_73c67c1b` metadata table.

**Does not mean:** Interpret only according to its columns and linked provenance; it is not an implicit graph relation.

## Keys and graph contract

- Primary/unique key: `not declared in source schema`
- Foreign keys/linkage: `none declared`

## Columns

| Column | Arrow dtype | Nullable |
|---|---|---:|
| `support_relation` | `large_string` | `true` |
| `mutation_id` | `large_string` | `true` |
| `support_target_id` | `large_string` | `true` |
| `support_target_type` | `large_string` | `true` |
| `source` | `large_string` | `true` |
| `source_dataset` | `large_string` | `true` |
| `source_record_id` | `large_string` | `true` |
| `study_id` | `large_string` | `true` |
| `evidence_score` | `double` | `true` |
| `effect_size` | `double` | `true` |
| `p_value` | `double` | `true` |
| `direction` | `large_string` | `true` |
| `predicate` | `large_string` | `true` |
| `text_span` | `large_string` | `true` |
| `support_class` | `large_string` | `true` |
| `is_context_only_support` | `bool` | `true` |

## Source, release, license and provenance

- Source: See table `source` columns and linked KG schema
- Release: Object generation/update date in this catalog; biological source release may be row-level
- License: Not derivable from Parquet metadata alone; consult source provenance before redistribution
- Provenance: [../../kg_schema_overview.md](../../kg_schema_overview.md)

## Layout and checksums

The logical dataset is represented by `1` physical Parquet object(s). Physical shards are packaging, not separate datasets.

| Object | Rows | Bytes | Generation | CRC32C | MD5 |
|---|---:|---:|---|---|---|
| `gs://jouvencekb/kg/v2/metadata/mutation_overlaps_enhancer_support_rows_t_73c67c1b.parquet` | 10,501 | 400,857 | `1782342625323144` | `Guac4g==` | `e63XJEqLZMXoIY64BiMGnA==` |
- catalog_manifest: [../inventory.json](../inventory.json)
- schema_doc: [../../kg_schema_overview.md](../../kg_schema_overview.md)

## Read examples

Requester-pays prerequisite (once public IAM permits it):

```bash
export BILLING_PROJECT='<your-gcp-billing-project>'
gcloud storage cp --billing-project="$BILLING_PROJECT" 'gs://jouvencekb/kg/v2/metadata/mutation_overlaps_enhancer_support_rows_t_73c67c1b.parquet' ./
```

PyArrow (GCS credentials/application-default credentials must carry the billing project):

```python
import os
import gcsfs
import pyarrow.dataset as ds
billing_project = os.environ['BILLING_PROJECT']
fs = gcsfs.GCSFileSystem(project=billing_project, requester_pays=billing_project)
paths = fs.glob('jouvencekb/kg/v2/metadata/mutation_overlaps_enhancer_support_rows_t_73c67c1b.parquet')
dataset = ds.dataset(paths, filesystem=fs, format='parquet')
print(dataset.head(5, columns=['support_relation']))
```

DuckDB:

```sql
-- Run after the requester-pays `gcloud storage cp` command above.
SELECT support_relation FROM read_parquet('./*.parquet') LIMIT 5;
```

## LaminDB / PyG linkage

This is a sidecar. Join through the declared node/edge/NCT keys; it is not topology unless a separate reviewed exporter explicitly consumes it.

## Caveats and leakage

Feature coverage is partial and source-dependent. Fit preprocessing only on the training partition and mask features that directly encode the evaluation target.
